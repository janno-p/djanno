from django.conf.urls import patterns, url

urlpatterns = patterns('coins.views',
    #url(r'^$', 'index'),
    #url(r'^(?P<coin_id>\d+)/$', 'detail'),
    #url(r'^(?P<coin_id>\d+)/results/$', 'results'),
    #url(r'^(?P<coin_id>\d+)/vote/$', 'vote'),
)
